

# app link flags
# once patched binutils with default link flags for user-space application
# these can be removed
app_link_flags = [
  '-z', 'noexecstack',
]

app_c_flags = [
] 

subdir('system_server')
